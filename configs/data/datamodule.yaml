# configs/data/graph_retrieval.yaml

_target_: src.data.datamodule.RetrievalDataModule

dataset_cfg: ${dataset}

# === Dataloader 逻辑参数 ===
batch_size: 2
num_workers: 4
eval_batch_size: 4
eval_num_workers: 4
pin_memory: true
train_shuffle: true