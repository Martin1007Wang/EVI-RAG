_target_: src.data.datamodule.RetrievalDataModule

dataset_cfg:
  name: ???
  data_dir: ???
  splits:
    train: train
    validation: validation
    test: test
  sample_limit: null
  sampler:
    # dataloader-level throttling; use trainer.limit_* for Lightning-level control
    train_batches_per_epoch: 8
    train_samples_per_epoch: null
  hard_negative_k: 4
  hard_negative_similarity: cosine

batch_size: 32
num_workers: 0
pin_memory: true
drop_last: true
persistent_workers: false
