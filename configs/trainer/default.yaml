_target_: lightning.pytorch.trainer.Trainer

default_root_dir: ${paths.output_dir}

max_epochs: 100000

accelerator: auto
devices: auto

# Default GPU training uses Lightning's bf16 mixed precision. Use 32-true only
# for CPU runs or when doing numerical debugging / parity checks.
precision: bf16-mixed

val_check_interval: 1.0
