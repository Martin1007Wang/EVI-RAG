# Single-GPU eval/test trainer (no DDP, no checkpointing).
_target_: lightning.pytorch.trainer.Trainer

accelerator: gpu
devices: 1
profiler: null
strategy: auto
precision: 32-true
max_epochs: 1
logger: null
enable_checkpointing: false
enable_progress_bar: true
deterministic: false
default_root_dir: ${paths.output_dir}
