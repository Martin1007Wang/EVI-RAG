# Canonical LLM post-hoc evaluation run.
# Use with `python src/eval.py ...`; `/dataset` is optional, but `/llm`
# must be configured (typically via `experiment=eval_llm`).
defaults:
  - default
  - _self_

name: eval_llm
task_name: "eval/llm"
tags: ["eval", "llm"]
