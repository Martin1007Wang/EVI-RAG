# @package _global_
#
# Default wiring for running an LLM over persisted GFlowNet rollouts (path prompts).
# Dataset and model backend should be overridden per experiment/CLI.

defaults:
  - override /stage: llm_reasoner_paths
  - override /logger: none

tags: ["llm_reasoner", "paths"]
