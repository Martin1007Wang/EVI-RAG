# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_11_budget6_db
experiment_name: canon_11_budget6_db

tags:
  - canonical_state
  - db
  - budget_ablation
  - budget_6

notes: >
  Budget ablation with canonical DB baseline.
  Tests whether budget=8 creates too much continuation pressure.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  budget: 6
    weak_replay_source:
      budget: ${model.budget}
