# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_08_frontier_corr10
experiment_name: canon_08_frontier_corr10

tags:
  - canonical_state
  - db
  - frontier_correction
  - correction_1_0

notes: >
  Tests full frontier-size correction.
  This may double-count correction effects with multi-parent backward,
  so it should be interpreted only against correction_0_0 and correction_0_5.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  policy:
    frontier_size_correction: 1.0
