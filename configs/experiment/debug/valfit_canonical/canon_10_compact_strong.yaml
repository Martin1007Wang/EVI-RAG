# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_10_compact_strong
experiment_name: canon_10_compact_strong

tags:
  - canonical_state
  - db
  - compactness
  - strong

notes: >
  Strong compactness penalty.
  Tests whether budget-sized redundant subgraphs remain a problem after
  canonical state removes permutation multiplicity.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  reward_model:
    edge_cost: 0.2
    redundant_edge_cost: 0.3
