# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_06_replay_strong
experiment_name: canon_06_replay_strong

tags:
  - canonical_state
  - db
  - stronger_replay

notes: >
  Increases weak replay strength to test whether failures are caused by poor
  rollout coverage rather than objective geometry.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  objective:
    nonterminal_weight: 1.0
    terminal_weight: 1.0
