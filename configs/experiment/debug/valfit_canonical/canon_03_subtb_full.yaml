# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_03_subtb_full
experiment_name: canon_03_subtb_full

tags:
  - canonical_state
  - subtb
  - full_segments

notes: >
  Full SubTB with all available subtrajectory lengths.
  Should only be compared after DB and short-SubTB baselines are understood.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  loss_fn:
    subtb_lambda: 0.8
    max_len: null
