# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_02_subtb_short
experiment_name: canon_02_subtb_short

tags:
  - canonical_state
  - subtb
  - short_segments

notes: >
  Enables short SubTB after DB baseline.
  Tests whether multi-step flow consistency improves credit propagation.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  loss_fn:
    subtb_lambda: 0.8
    max_len: 4
