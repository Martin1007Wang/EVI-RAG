# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: canon_09_compact_loose
experiment_name: canon_09_compact_loose

tags:
  - canonical_state
  - db
  - compactness
  - loose

notes: >
  Weak compactness penalty.
  Tests whether current compactness is suppressing necessary multi-hop evidence.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_canonical
  tags: ${tags}

model:
  reward_model:
    edge_cost: 0.05
    redundant_edge_cost: 0.1
