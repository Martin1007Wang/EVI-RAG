# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: valfit_degrade_terminal_weak
experiment_name: valfit_degrade_terminal_weak

tags:
  - valfit_degrade
  - objective
  - weaker_terminal

notes: >
  Reduces direct STOP-to-reward fitting strength.
  Tests whether late regression is caused by terminal reward matching
  overpowering nonterminal expansion supervision.

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: valfit_degrade
  tags: ${tags}

model:
  objective:
    terminal_weight: 0.5
