# @package _global_

defaults:
  - /experiment/debug/valfit
  - _self_

task_name: debug_valfit_no_replay
experiment_name: debug_valfit_no_replay

tags:
  - debug
  - valfit
  - no_replay
  - policy_only

logger:
  name: ${experiment_name}-${dataset.name}-${now:%Y%m%d-%H%M%S}
  group: debug_valfit
  tags: ${tags}

model:
  runner:
    replay_source: null
