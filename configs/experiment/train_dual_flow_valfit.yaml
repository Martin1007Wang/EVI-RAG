# @package _global_
#
# Train/val on the validation split for fast verification.

defaults:
  - train_dual_flow
  - _self_

data:
  splits:
    train: validation
    validation: validation
    test: validation
