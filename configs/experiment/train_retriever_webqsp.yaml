# @package _global_

defaults:
  - train_retriever_default
  # Train on the filtered dataset to guarantee valid graphs/supervision.
  - override /dataset: webqsp-sub

tags: ["webqsp", "train_retriever"]
