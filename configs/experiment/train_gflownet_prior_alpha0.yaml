# @package _global_

defaults:
  - train_gflownet_default
  - override /dataset: webqsp

model:
  embedder_cfg:
    projector_checkpoint: "logs/train_retriever_webqsp/runs/2025-12-02_19-26-32/checkpoints/epoch_094.ckpt"
  training_cfg:
    retriever_prior_alpha: 0.0            # 只在构图阶段用 retriever，不叠加 logit 先验
    retriever_prior_anneal: null
    pb_loss_weight: 1.0
    pb_loss_anneal:
      start: 1.0
      end: 0.0
      steps: 5000

task_name: "train_gflownet_prior_alpha0"
tags: ["ablation", "prior=0"]
