# @package _global_
#
# Experiment: reasoning over GFlowNet rollouts (path prompts).

defaults:
  - override /data: reasoner_paths
  - override /model: reasoner/ollama
  - override /callbacks: none
  - override /logger: none
  - override /trainer: predict

run:
  name: reasoner_paths
  task_name: "reasoner/paths"
  tags: ["eval", "reasoner", "paths"]
  split: test
  ckpt_path: null
  dataset_variants:
    - ${dataset.dataset_family}
    - ${dataset.dataset_family}-sub
  require_dual_datasets: true
