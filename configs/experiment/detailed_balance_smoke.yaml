# @package _global_

defaults:
  - override /model: weaver
  - override /trainer: fast_dev
  - override /logger: none
  - _self_

task_name: detailed_balance_smoke

model:
  runner:
    train_policy_rollouts: 1
    eval_rollouts: 1
