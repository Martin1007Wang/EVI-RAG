# @package _global_
#
# Fast-iteration bundle for the guided answer-committed mainline.

defaults:
  - train_rankflow_guided
  - train_rankflow_fastiter
  - _self_

run:
  name: train_rankflow_guided_fastiter
  task_name: "train/rankflow_guided_fastiter"
  tags: ["rankflow", "gflownet", "answer-commit", "guided", "replay", "fastiter"]

model:
  training_cfg:
    auxiliary:
      replay:
        buffer:
          replay_trajectories_per_step: 16
