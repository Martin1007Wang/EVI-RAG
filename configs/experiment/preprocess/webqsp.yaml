# @package _global_

defaults:
  - override /dataset: webqsp
  - override /preprocess: default
  - _self_

task_name: preprocess_webqsp

preprocess:
  encoder:
    question_prefix: "Represent this sentence for searching relevant passages: "
