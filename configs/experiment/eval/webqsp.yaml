# @package _global_

defaults:
  - override /dataset: webqsp
  - override /model: weaver
  - override /trainer: gpu
  - override /logger: wandb
  - _self_

task_name: eval_webqsp

logger:
  tags: [weaver, eval, webqsp]

model:
  runner:
    eval_num_rollouts: 32
  evaluation:
    k_windows: [1, 2, 4, 8, 16, 32]
