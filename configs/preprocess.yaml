# @package _global_
defaults:
  - _self_
  - paths: default
  - dataset: null
  - pipeline: default
  - hydra: default

task_name: preprocess

progress_bar: true

encoder:
  model_name: BAAI/bge-large-en-v1.5
  device: auto
  batch_size: 512

hf_env:
  cache_dir: ${paths.hf_datasets_cache}
  offline: false