# @package model.training_cfg.auxiliary.replay

enabled: false
mix_alpha: 0.0

buffer:
  capacity: 1024
  min_buffer_size: 64
  replay_trajectories_per_step: null
  deduplicate: true

guidance:
  # Reference-path replay and expand-imitation controls.
  add_shortest_path_guidance: false
  expand_imitation_weight: 0.0
  expand_imitation_from_anchor_bonus: 0.0
  expand_imitation_answer_finish_bonus: 0.0
  mask_stop_loss: true

schedule:
  type: constant
  initial_alpha: null
  final_alpha: null
  total_steps: null
  hold_steps: 0
