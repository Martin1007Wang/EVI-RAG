# @package model.eval_cfg

defaults:
  - _self_
  - monte_carlo: default

report_profile: full
task: answer_ranking
answer_mass_threshold: 0.9
support_mass_threshold: 0.9
support_path_overlap_penalty: 0.25
answer_top_ks: [1, 5, 10]
edge_top_ks: [1, 5, 10, 25, 50]
edge_emit_top_k: 25
