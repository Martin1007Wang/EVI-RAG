# EVI-RAG 当前实现的算法逻辑

## 1. 一句话概括

这套代码实现的是一个 **基于 GFlowNet 的知识图谱证据子图检索器**：

- 输入是问题 `q` 和局部知识图谱 `G=(V,E)`
- 输出不是一个单点答案，而是一个 **小型证据子图** `x=(V_x,E_x)`
- 目标是让这个子图：
  - 尽量覆盖答案节点
  - 尽量少引入无关边

可以把它理解为：

> 从问题实体出发，在图上一步步扩展证据，直到得到一个既能覆盖答案、又尽量紧凑的子图。

---

## 2. 问题形式化

对每个样本，给定：

- 问题：`q`
- 有向局部图：

  ```math
  G=(V,E)
  ```

- 问题中出现并对齐到图中的实体集合，记为 **锚点集合**：

  ```math
  A \subseteq V
  ```

- 图中的真实答案实体集合，记为 **目标集合**：

  ```math
  Y \subseteq V
  ```

模型要学习一个分布：

```math
P(x\mid q),\quad x=(V_x,E_x),\ V_x\subseteq V,\ E_x\subseteq E
```

并希望满足：

```math
P(x\mid q) \propto R(x)
```

即：**终止子图越好，采样到它的概率越大。**

---

## 3. 预处理阶段到底做了什么

预处理不是在训练模型，而是在为后续图搜索准备结构信息。

它主要构造四类信息：

### 3.1 局部图

把原始三元组集合整理成局部图：

```math
E = \{(u,r,v)\}
```

并将实体、关系映射成离散编号。

### 3.2 锚点与目标

- 锚点：问题里提到且出现在图中的实体
- 目标：答案里出现在图中的实体

### 3.3 锚点距离

对每个节点 `v`，计算：

- 从任一锚点到 `v` 的最短前向距离
- 从 `v` 到任一锚点的最短反向距离

可写为：

```math
d_A^{\rightarrow}(v)=\min_{a\in A} d(a\to v)
```

```math
d_A^{\leftarrow}(v)=\min_{a\in A} d(v\to a)
```

### 3.4 目标最短路径标签

对每个目标 `y`，计算：

- 每个节点到 `y` 的最短距离：

  ```math
  d_y(v)=d(v\to y)
  ```

- 从节点 `v` 到 `y` 的最短后缀路径条数
- 哪些边位于“锚点到目标”的某条最短路径上

这一步的作用是：

> 让系统知道哪些边在结构上更可能是“通往答案”的有效证据。

---

## 4. 状态：当前已经长出的子图

算法不是一次性选出所有边，而是逐步构建子图。

第 `t` 步访问到的状态记为：

```math
s_t=(V(E_t),E_t)
```

其中：

- `E_t`：当前已激活边集合
- `V(E_t)`：由当前边集合诱导出的节点集合，可写为

  ```math
  V(E_t)=A\cup\{u,v:(u,r,v)\in E_t\}
  ```

- rollout budget 特征不是独立状态变量，而是由非 root 已选边数推出：

  ```math
  k(s_t)=|E_t\setminus E_0|
  ```

### 4.1 初始状态

初始时：

- 激活节点就是锚点：

  ```math
  V_0=A
  ```

- 激活边是锚点之间本来就存在的边：

  ```math
  E_0=\{(u,v)\in E\mid u\in A,\ v\in A\}
  ```

所以它并不是“从空图开始”，而是：

> 从问题已经明确指出的实体出发，向外扩展证据。

---

## 5. 动作空间：停还是继续扩边

每一步只有两类动作：

### 5.1 Stop

直接停止，当前子图就是最终输出。

### 5.2 Expand(e)

选择一条候选边 `e` 加入当前子图。

候选边不是任意边，而是当前边界上的边：

```math
C_t=\{e=(u,v)\in E\setminus E_t \mid u\in V_t \ \text{or} \ v\in V_t\}
```

也就是：

> 至少有一个端点已经在当前子图里，但这条边还没有被选过。

这保证了子图是“从当前证据向外生长”的，而不是跳到图中无关位置。

---

## 6. 状态转移：选择一条边后会发生什么

若第 `t` 步选择扩展边：

```math
e=(u,v)
```

则：

```math
E_{t+1}=E_t\cup\{e\}
```

```math
V_{t+1}=V_t\cup\{u,v\}
```

并且时间步加一。

若选择 `Stop`，轨迹终止。

因此，这个算法本质上在做一个有限步图搜索：

> 不断向当前子图边界添加边，直到决定停下。

---

## 7. 策略网络：如何判断该选哪条边

模型会先构造一个当前状态的表示：

```math
h_s = f(q, V(E_t), E_t, A, \phi(s_t))
```

其中：

- `q` 是问题向量
- `V(E_t), E_t` 表示当前已选子图
- `A` 是锚点信息
- `\phi(s_t)` 是从当前子图派生的简单特征，例如：
  - 已激活节点占比
  - 已扩展边占比
  - 由 `|E_t \setminus E_0|` 推出的剩余预算占比

也就是说，模型不是只看一条边，而是看：

> 在当前已经长出的证据背景下，再加哪条边最合理。

---

## 8. 候选边打分公式

对每条候选边：

```math
e=(u,r,v)
```

模型计算一个分数：

```math
\ell_e(s,q)=\text{semantic}(q,e)+\text{state}(q,h_s,e)+\text{structural}(s,e)
```

三部分含义分别是：

### 8.1 语义项

衡量问题和这条边的语义相关性，例如：

- 问题是否和关系 `r` 匹配
- 问题是否和源点/终点语义匹配

### 8.2 状态项

衡量这条边在当前状态下是否合适，例如：

- 当前已选证据已经覆盖了什么
- 这条边是否能补充新的有用信息

### 8.3 结构项

衡量这条边在图结构上是否合理，例如：

- 它是否沿着锚点到答案的合理方向推进
- 它与当前活跃节点的结构关系如何
- 当前预算还剩多少

因此，一条边得分高，不是因为它“看起来像答案”，而是因为它：

> 在语义、当前上下文、图结构三方面都更像一条好证据边。

---

## 9. Stop 动作打分

停止动作也有一个单独分数：

```math
\ell_{\text{stop}}(s,q)=g(q,h_s,\phi(s_t))
```

它表达的是：

> 当前证据是否已经足够，是否应该停止继续扩边。

注意它不是“奖励值”，只是一个动作打分。

这里算法仍然通过 rollout 生成轨迹

```math
\tau:s_0\to s_1\to \cdots \to s_T
```

但每个 `s_t` 都是 canonical subgraph state，而不是 history state。终止对象是

```math
x=s_T
```

训练时，Trajectory Balance / SubTB 约束的是状态流与终止奖励的匹配，即 `F(x)=R(x)` 的一致性，而不是把某一条具体构造轨迹直接当作唯一训练对象。

---

## 10. 前向策略概率

先把所有候选扩边的总分聚合成 Expand 的总质量：

```math
L_{\text{expand}}(s)=\log\sum_{e\in C_t}\exp(\ell_e)
```

于是，先决定 **停** 还是 **扩**：

```math
P(\text{Stop}\mid s)=
\frac{\exp(\ell_{\text{stop}})}{\exp(\ell_{\text{stop}})+\exp(L_{\text{expand}}(s))}
```

```math
P(\text{Expand}\mid s)=
\frac{\exp(L_{\text{expand}}(s))}{\exp(\ell_{\text{stop}})+\exp(L_{\text{expand}}(s))}
```

如果决定扩展，再在候选边中选择具体边：

```math
P(e\mid s,\text{Expand})=
\frac{\exp(\ell_e)}{\sum_{e'\in C_t}\exp(\ell_{e'})}
```

所以一条 Expand 动作的前向概率为：

```math
P_F(\text{Expand}(e)\mid s)=P(\text{Expand}\mid s)\cdot P(e\mid s,\text{Expand})
```

而停止动作概率为：

```math
P_F(\text{Stop}\mid s)=P(\text{Stop}\mid s)
```

---

## 11. 奖励函数：什么子图才算好

只有当轨迹停止时，才计算奖励。

设终止子图为：

```math
x=(V_x,E_x)
```

### 11.1 有效检索节点集合

代码里不会让固定锚点平白拉低精度，因此定义：

```math
P_x = V_x \setminus (A \setminus Y)
```

含义是：

- 如果某个锚点不是答案，它不应白白占据 precision 分母
- 如果某个锚点本身也是答案，仍然应计为命中

### 11.2 精度、召回率、F1

```math
\text{Precision}(x)=\frac{|P_x\cap Y|}{|P_x|}
```

```math
\text{Recall}(x)=\frac{|P_x\cap Y|}{|Y|}
```

```math
F1(x)=\frac{2\cdot \text{Precision}(x)\cdot \text{Recall}(x)}{\text{Precision}(x)+\text{Recall}(x)}
```

### 11.3 边惩罚

根边 `E_0` 不算学习得到的扩展边，真正被惩罚的是额外扩出来的边：

```math
|E_x \setminus E_0|
```

若扩展预算为 `B`，则默认惩罚项近似为：

```math
\text{penalty}(x)=\lambda \cdot \frac{|E_x\setminus E_0|}{B}
```

### 11.4 最终对数奖励

```math
\log R(x)=
\max
\left(
\log(F1(x)) - \text{penalty}(x),
\text{clip}_{\min}
\right)
```

如果 `F1=0`，代码不会取 `log 0`，而是使用一个固定负值作为零命中奖励。

因此奖励机制鼓励的是：

> 找到答案，同时尽量少走冤枉路。

---

## 12. 为什么需要反向概率

这是一个 GFlowNet，因此不仅定义前向生成过程，也定义反向过程。

反向过程不是重新生成图，而是：

> 从当前子图中删除一条“可合法删除的非根边”。

如果当前状态 `s'` 中，可合法删除的边集合为：

```math
R_{\text{valid}}(s')
```

则反向概率定义为均匀分布：

```math
P_B(s\mid s')=\frac{1}{|R_{\text{valid}}(s')|}
```

这里“合法删除”并不是随便删，而是要求删完后剩余边集仍然能按前向规则从锚点逐步构造出来。

这一步的意义是：

> 让前向生长过程和反向回退过程都可计算，从而建立概率流守恒约束。

---

## 13. 训练目标：SubTrajectory Balance

当前实现使用的是 **SubTrajectory Balance** 损失，而不是普通交叉熵。

模型还会学习两个标量量：

- 根状态的流量：

  ```math
  \log Z(q)
  ```

- 中间状态的流量：

  ```math
  \log F(s)
  ```

对任意子轨迹：

```math
s_i \xrightarrow{a_i} s_{i+1} \xrightarrow{a_{i+1}} \cdots \xrightarrow{a_j} s_{j+1}
```

定义残差：

```math
\Delta_{i,j}=
\log F(s_i)
+ \sum_{t=i}^{j}\big(\log P_F(a_t\mid s_t)-\log P_B(s_t\mid s_{t+1})\big)
- T_j
```

其中目标项 `T_j` 为：

- 若 `j` 后面还有状态，则：

  ```math
  T_j=\log F(s_{j+1})
  ```

- 若 `j` 是终止 Stop 步，则：

  ```math
  T_j=\log R(x)
  ```

训练就是最小化这些残差平方和。

这表示模型在学习：

> 任意一段局部轨迹，其前向概率质量、反向概率质量、状态流量、终止奖励之间必须彼此一致。

这正是 GFlowNet 的核心约束。

---

## 14. 推理时模型怎么工作

训练好后，对同一个问题不会只生成一次，而是会采样多条轨迹。

每条轨迹都会得到一个终止子图：

```math
x^{(1)},x^{(2)},\dots,x^{(K)}
```

随后可以做两类使用：

### 14.1 单次采样使用

把某一次采样出的子图直接作为证据子图。

### 14.2 多次采样聚合

把多次采样得到的子图求并集，形成更完整的证据覆盖：

```math
V_{\text{union}}=\bigcup_{k=1}^K V_{x^{(k)}}
```

```math
E_{\text{union}}=\bigcup_{k=1}^K E_{x^{(k)}}
```

这对应代码中的 union subgraph 输出。

---

## 15. 评估指标在衡量什么

当前评估大致分两类：

### 15.1 Sample 平均指标

表示：

> 如果从当前策略中随机采样一个子图，它平均能有多好。

主要包括：

- 平均 target precision
- 平均 recall
- 平均 target F1
- 平均节点数、边数、非根边数

### 15.2 Best-of-k 指标

表示：

> 如果给模型 `k` 次采样机会，它至少有一次采到好子图的能力如何。

例如：

- `max_recall_at_k`
- `max_target_f1_at_k`

这反映的是模型的 **探索能力**，而不仅是单次命中率。

---

## 16. 当前默认实现的关键特征

按当前默认配置，这个实现有几个重要特点：

### 16.1 默认是有限预算扩展

默认扩展预算较小，意味着模型必须在少量步数内选择高价值边。

### 16.2 默认奖励是终止奖励

中间步骤没有单独奖励，只有停止时才根据最终子图质量打分。

### 16.3 默认基线不启用 coverage proposal

当前主基线配置中，coverage-guided proposal 默认关闭，因此主流程主要依赖模型自身学出的前向策略分布。

### 16.4 目标不是单路径，而是子图分布

算法追求的不是“唯一最优路径”，而是：

> 让高质量证据子图整体获得更高概率质量。

---

## 17. 最终本质总结

这套代码实现的算法，本质上可以压缩成下面四句话：

1. 从问题实体锚点出发，把局部知识图谱看成一个可逐步扩展的搜索空间。
2. 每一步在“停止”与“扩一条边”之间做概率决策，逐步构造证据子图。
3. 终点子图如果更能覆盖答案、且更紧凑，就获得更高奖励。
4. 用 GFlowNet 的概率流一致性训练，使高奖励子图被更高概率采样到。

因此，算法真正学习到的是：

```math
\text{如何在知识图谱中生成“高质量证据子图”的概率分布。}
```

而不是单纯学习一个“答案分类器”或“一条最短路径搜索器”。
